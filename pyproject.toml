[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "riskmeasures"
version = "0.1.0"
description = "EVaR, CVaR, and VaR risk measures with empirical estimation"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "scipy",
]
